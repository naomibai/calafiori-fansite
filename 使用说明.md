# 卡拉菲奥里球迷站 · 使用说明

> 不用懂代码也能维护这个网站。照着下面的步骤做就行。

---

## 🌐 网站永久链接

**https://naomibai.github.io/calafiori-fansite/**

任何人都可以用浏览器打开这个链接访问你的网站，永久有效、不会过期。
每次网站更新后，链接不变，刷新即可看到新版。

---

## 一、怎么打开网站

双击文件夹里的 **`index.html`**，它会在浏览器（Chrome / Edge 都可以）里打开。

---

## 二、怎么换首页视频背景

1. 把你准备好的视频文件**复制**到 `assets/video/` 文件夹里；
2. 把文件名改成 **`hero.mp4`**；
3. 回到浏览器按 `F5` 刷新，首页就会播放你的视频。

> 没有放视频时，首页会自动显示红白海报背景，不影响浏览。

---

## 三、怎么换图片

图库照片现在都收集自 Pinterest 粉丝分类板，按时期分文件夹存放在本地：

```
assets/img/
├── roma/       ← 罗马青训时期的照片
├── basel/      ← 巴塞尔时期的照片
├── bologna/    ← 博洛尼亚时期的照片
├── arsenal/    ← 阿森纳时期的照片
├── italy/      ← 意大利国家队的照片
└── offpitch/   ← 场外花絮照片
```

### 换掉某张照片

1. 找到你想换掉的照片文件，记下它的文件名（比如 `assets/img/arsenal/arsenal-01.jpg`）；
2. 把你的新图片**重命名成一样的文件名**，复制进同一个文件夹覆盖它；
3. 刷新网页即可，不需要改任何代码。

### 增加新的照片

1. 把你的图片复制到对应时期的文件夹里（建议文件名用英文或数字，如 `arsenal-25.jpg`）；
2. 用记事本打开 `index.html`，找到该时期图库分组（搜索 `gallery-arsenal` 等），照着组内已有照片的样子，复制一段 `<figure> ... </figure>` 代码，把里面的文件名改成你的新文件名；
3. 保存并刷新。

> 想批量加图、或者怕改坏代码？把新照片放进对应文件夹后直接告诉我，我来帮你加。

---

## 四、怎么添加采访视频（自动生成中文文字稿）

1. 把你的采访视频文件（mp4 格式）复制进 `assets/video/interviews/` 文件夹；
2. 告诉我一声「新视频加好了」，我会自动：
   - 用 AI 把视频里的语音**转写成文字**（意大利语、英语都能识别）
   - **翻译成中文文字稿**
   - 自动截取视频画面做**封面图**
   - 把视频卡片和文字稿加到网页并发布上线
3. 刷新网站链接就能看到新视频和文字稿。

> 想自己修改文字稿：用记事本打开 `index.html`，搜索「中文文字稿」，直接改里面的文字即可。

---

## 五、用手机上传照片 / 视频（云端档案模式）

网站已支持「上传即显示」：从手机上把照片或视频传到 GitHub 仓库里，网站会自动展示出来，不需要改任何代码。

### 手机上传照片

1. 手机浏览器打开你的仓库：https://github.com/naomibai/calafiori-fansite （需登录 GitHub 账号）
2. 依次进入文件夹：`assets` → `img` → 选择时期（如 `arsenal`）
3. 点右上角「**Add file**」→「**Upload files**」
4. 从手机相册选择照片 → 点「**Commit changes**」提交
5. 等约 1 分钟后，刷新网站链接，新照片自动出现在图库中 ✔

### 手机上传视频

1. 同样在手机浏览器打开仓库 → 进入 `assets` → `video` → `interviews`
2. 「Add file」→「Upload files」→ 从相册选择视频（**单个视频需小于 100MB**）
3. 提交后刷新网站，「视频采访」栏自动出现新视频卡片（封面缺失时显示占位色块）
4. 新视频的中文文字稿需要我来生成：上传后跟我说一声，我转写 + 翻译后，文字稿会自动出现在网站

> 提示：
> - 视频超过 100MB 无法直接手机上传：传到电脑上放进 `assets/video/interviews/` 文件夹，告诉我即可
> - 手机访问 GitHub 不方便时，也可以把照片/视频发到电脑（微信文件传输助手等），放进对应文件夹后告诉我，我来上传
> - 新上传的照片想要删掉或调整位置，随时告诉我

---

## 六、怎么改文字

所有文字都在 `index.html` 里，用记事本打开就能改。常用的：

- **首页大标语**：搜「钢铁防线」
- **球员名字**：搜「CALAFIORI」
- **时期简介**：搜「罗马青训」等
- **底部标语**：搜「仅供粉丝交流」

改完保存 → 浏览器按 `F5` 刷新。

---

## 七、文件夹是干什么的

```
网页/
├── index.html       ← 网页内容（所有文字、图片、链接都在这里改）
├── styles.css       ← 网页样式（颜色、字号、排版；一般不用动）
├── script.js        ← 网页功能（切换、放大等；一般不用动）
├── assets/
│   ├── video/       ← 首页背景视频（hero.mp4）＋ 采访视频（interviews/）
│   ├── img/         ← 图库照片（按时期分文件夹）＋ 视频封面（covers/）
│   ├── transcripts/ ← 采访视频的中文文字稿（.txt，自动生成）
│   └── fonts/       ← 网站字体文件（Bebas Neue），不要删除
└── 使用说明.md       ← 就是本说明
```

---

## 八、常见问题

**问：图库照片的来源？**
答：照片收集自 Pinterest 粉丝分类主页，版权归原作者所有。网站底部已注明「仅供粉丝交流使用，不允许商用」。如果原作者要求删除某张照片，告诉我，我可以随时撤下或替换。

**问：手机上传后网站多久更新？**
答：GitHub Pages 一般在 1 分钟左右自动重新发布；网页每次打开都会自动读取仓库的文件清单，所以新上传的照片/视频随后就会自动出现在网页上（刷新一下即可）。

**问：想改颜色？**
答：颜色都集中在 `styles.css` 最顶部的 `:root` 里（三个红色值），改数字即可。拿不准就告诉我，我来帮你改。

**问：想要更多板块（比如荣誉墙、比赛集锦列表）？**
答：随时告诉我，我来加。

**问：想让朋友也能看到这个网站？**
答：网站已经发布到网上啦！把永久链接发给朋友即可：**https://naomibai.github.io/calafiori-fansite/**
