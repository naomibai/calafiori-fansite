# 卡拉菲奥里球迷站 · 使用说明

> 不用懂代码也能维护这个网站。照着下面的步骤做就行。

---

## 🌐 网站永久链接

**https://naomibai.github.io/calafiori-fansite/**

任何人都可以用浏览器打开这个链接访问你的网站，永久有效、不会过期。
每次网站更新后，链接不变，刷新即可看到新版。

---

## 一、怎么打开网站

双击文件夹里的 **`index.html`**，它会在浏览器（Chrome / Edge 都可以）里打开。

---

## 二、怎么换首页视频背景

1. 把你准备好的视频文件**复制**到 `assets/video/` 文件夹里；
2. 把文件名改成 **`hero.mp4`**；
3. 回到浏览器按 `F5` 刷新，首页就会播放你的视频。

> 没有放视频时，首页会自动显示红白海报背景，不影响浏览。

---

## 三、怎么换图片

网页里现在有两种图：

| 位置 | 现在是什么 | 怎么换成你的图 |
|------|-----------|--------------|
| 「个人经历」卡片图 | 网络占位图 / 红白占位块 | 见下方「方法一」 |
| 「图库」照片 | 网络占位图 / 「照片待补充」占位块 | 见下方「方法一」 |

### 方法一：换成你自己电脑里的图片

1. 把你的图片**复制**到 `assets/img/` 文件夹里（建议文件名用英文或数字，比如 `arsenal-1.jpg`）；
2. 用**记事本**打开 `index.html`；
3. 找到你要替换的位置。所有需要改的地方，代码里都有 `★` 星号注释提示；
4. 把 `src="……"` 引号里的网址，改成 `assets/img/你的文件名.jpg`；

   例如，把：
   ```html
   <img src="https://commons.wikimedia.org/...很长的网址...">
   ```
   改成：
   ```html
   <img src="assets/img/arsenal-1.jpg">
   ```
5. 保存文件，回到浏览器按 `F5` 刷新。

### 方法二：填充「照片待补充」的占位块

图库里标着「照片待补充」的格子，是没有图片的空位。给它填图：

1. 把图片放进 `assets/img/` 文件夹；
2. 在 `index.html` 里找到这个格子（它里面有 `照片待补充` 几个字）；
3. 在 `<figure class="photo ...">` 和 `<div class="photo-missing">` 之间，插入一行图片代码：

   ```html
   <img src="assets/img/你的文件名.jpg"
        data-full="assets/img/你的文件名.jpg"
        data-caption="照片说明文字">
   ```
4. 保存并刷新。占位块会自动变成你的照片。

---

## 四、怎么换采访链接和文字稿

1. 用记事本打开 `index.html`；
2. 找到「采访」板块（页面里搜 `YouTube 链接`）；
3. **换视频链接**：把 `href="https://www.youtube.com/..."` 引号里的网址，换成真实的采访视频网址；
4. **换文字稿**：把 `【占位文字】` 那几段，换成真实的采访中文稿；
5. 标题、来源、日期这些文字直接改就行。

---

## 五、怎么改文字

所有文字都在 `index.html` 里，用记事本打开就能改。常用的：

- **首页大标语**：搜「钢铁防线」
- **球员名字**：搜「CALAFIORI」
- **时期简介**：搜「罗马青训」等
- **底部标语**：搜「仅供粉丝交流」

改完保存 → 浏览器按 `F5` 刷新。

---

## 六、文件夹是干什么的

```
网页/
├── index.html       ← 网页内容（所有文字、图片、链接都在这里改）
├── styles.css       ← 网页样式（颜色、字号、排版；一般不用动）
├── script.js        ← 网页功能（切换、放大等；一般不用动）
├── assets/
│   ├── video/       ← 放首页背景视频（hero.mp4）
│   └── img/         ← 放你自己的图片
└── 使用说明.md       ← 就是本说明
```

---

## 七、常见问题

**问：网页里的网络占位图显示不出来？**
答：这些是临时占位图，不影响使用。等换成你自己电脑里的图片（方法一）就没这个问题了。或者直接告诉我，我可以帮你把整站图片都改成本地文件。

**问：想改颜色？**
答：颜色都集中在 `styles.css` 最顶部的 `:root` 里（三个红色值），改数字即可。拿不准就告诉我，我来帮你改。

**问：想要更多板块（比如荣誉墙、比赛集锦列表）？**
答：随时告诉我，我来加。

**问：想让朋友也能看到这个网站？**
答：网站已经发布到网上啦！把永久链接发给朋友即可：**https://naomibai.github.io/calafiori-fansite/**
